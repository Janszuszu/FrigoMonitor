# FrigoMonitor Frontend

Vue 3 dashboard for FM-015.

## Development

```powershell
npm install
npm run dev
```

Default API endpoints:

- REST: `http://localhost:8000/api/v1`
- WebSocket: `ws://localhost:8000/ws`

Override them with `.env` values based on `.env.example`.
